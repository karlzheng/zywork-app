<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.zywork.dao.UserInviteDAO">

    <insert id="saveUserHierarchy">
       call invite_user(#{pUserId}, #{userId})
    </insert>

</mapper>
