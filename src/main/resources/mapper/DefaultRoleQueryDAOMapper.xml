<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.zywork.dao.DefaultRoleQueryDAO">

    <select id="getDefaultRole" resultType="java.lang.Long">
        select id from t_role where is_default = 1
    </select>

</mapper>
